# Android 项目结构说明文档

## 📁 项目整体结构

```
demo2/
├── .gradle/                    # Gradle 构建系统的缓存目录（自动生成）
├── .idea/                      # Android Studio IDE 的配置文件（自动生成）
├── app/                        # 应用模块目录（主要开发目录）
├── build/                      # 项目构建输出目录（自动生成）
├── gradle/                     # Gradle 包装器和依赖版本管理
├── .gitignore                  # Git 版本控制忽略文件配置
├── build.gradle.kts           # 项目级别的构建配置文件
├── gradle.properties          # Gradle 全局属性配置
├── gradlew                    # Linux/Mac 的 Gradle 包装器脚本
├── gradlew.bat                # Windows 的 Gradle 包装器脚本
├── local.properties           # 本地环境配置（SDK 路径等）
└── settings.gradle.kts        # Gradle 项目设置文件
```

---

## 🔧 根目录文件详解

### 1. `build.gradle.kts`
**作用**: 项目级别的构建配置文件，定义所有子模块共用的配置
- 配置 Android Gradle 插件版本
- 定义项目级别的依赖仓库
- 配置所有模块共享的构建选项

### 2. `settings.gradle.kts`
**作用**: Gradle 项目设置文件
- 定义项目名称
- 配置插件管理仓库（Google、Maven Central）
- 声明包含的模块（`:app`）
- 配置依赖解析策略

### 3. `gradle.properties`
**作用**: Gradle 全局属性配置
- JVM 内存设置（`org.gradle.jvmargs=-Xmx2048m`）
- 启用 AndroidX 库（`android.useAndroidX=true`）
- 优化构建性能的配置

### 4. `local.properties`
**作用**: 本地环境配置（不应提交到版本控制）
- Android SDK 路径
- NDK 路径（如果使用）
- 每个开发者的本地配置

### 5. `gradlew` 和 `gradlew.bat`
**作用**: Gradle 包装器脚本
- 确保所有开发者使用相同版本的 Gradle
- 无需手动安装 Gradle
- `gradlew` 用于 Linux/Mac，`gradlew.bat` 用于 Windows

---

## 📦 gradle/ 目录

### `gradle/libs.versions.toml`
**作用**: 版本目录管理（Version Catalog）
- 集中管理所有依赖库的版本号
- 避免版本冲突
- 便于统一升级依赖

**结构**:
- `[versions]`: 定义版本号变量
- `[libraries]`: 定义依赖库
- `[plugins]`: 定义 Gradle 插件

### `gradle/wrapper/`
**作用**: Gradle 包装器文件
- `gradle-wrapper.jar`: Gradle 包装器可执行文件
- `gradle-wrapper.properties`: Gradle 版本配置

---

## 📱 app/ 模块目录（核心开发目录）

```
app/
├── build.gradle.kts           # 模块级别的构建配置
├── proguard-rules.pro         # 代码混淆规则
├── .gitignore                 # 模块的 Git 忽略配置
└── src/                       # 源代码目录
    ├── main/                  # 主代码目录
    │   ├── java/              # Java/Kotlin 源代码
    │   ├── res/               # 资源文件
    │   └── AndroidManifest.xml # 应用清单文件
    ├── test/                  # 单元测试目录
    └── androidTest/           # Android 仪器测试目录
```

### `app/build.gradle.kts`
**作用**: 应用模块的构建配置文件
- **namespace**: 应用包名（`com.example.demo2`）
- **compileSdk**: 编译使用的 Android SDK 版本（36）
- **minSdk**: 支持的最低 Android 版本（24 = Android 7.0）
- **targetSdk**: 目标 Android 版本（36）
- **versionCode**: 应用版本号（整数，用于版本比较）
- **versionName**: 应用版本名称（字符串，显示给用户）
- **dependencies**: 应用依赖的库

### `app/proguard-rules.pro`
**作用**: ProGuard 代码混淆和优化规则
- 保护代码不被反编译
- 移除未使用的代码
- 优化字节码

---

## 📂 src/main/ 主代码目录

### `AndroidManifest.xml`
**作用**: 应用清单文件（最重要的配置文件之一）
- 声明应用的包名
- 定义应用图标、名称、主题
- 声明所有 Activity、Service、BroadcastReceiver
- 配置权限
- 定义启动 Activity（`LAUNCHER`）

### `java/` 目录
**作用**: 存放 Java/Kotlin 源代码
- 按包名组织（`com/example/demo2/`）
- 包含所有 Activity、Fragment、工具类等

**当前文件**:
- `MainActivity.java`: 应用的主界面 Activity

### `res/` 资源目录

#### `res/layout/`
**作用**: 存放 XML 布局文件
- 定义界面的视图结构
- `activity_main.xml`: MainActivity 的布局文件

#### `res/values/`
**作用**: 存放各种值资源
- `strings.xml`: 字符串资源（支持多语言）
- `colors.xml`: 颜色值定义
- `themes.xml`: 应用主题样式

#### `res/values-night/`
**作用**: 夜间模式的资源
- 包含深色主题的配置

#### `res/drawable/`
**作用**: 存放图片和可绘制资源
- PNG、JPG、XML 矢量图等

#### `res/mipmap-*/`
**作用**: 存放应用图标
- 不同分辨率的图标（hdpi、xhdpi、xxhdpi 等）
- `ic_launcher.png`: 应用图标
- `ic_launcher_round.png`: 圆形图标

#### `res/xml/`
**作用**: 存放 XML 配置文件
- `backup_rules.xml`: 备份规则
- `data_extraction_rules.xml`: 数据提取规则

---

## 🧪 测试目录

### `src/test/`
**作用**: 单元测试目录
- 运行在 JVM 上的本地测试
- 不需要 Android 设备或模拟器
- 测试业务逻辑、工具类等

### `src/androidTest/`
**作用**: Android 仪器测试目录
- 运行在 Android 设备或模拟器上
- 测试 UI、Activity、数据库等
- 需要 Android 环境

---

## 🔑 关键概念解释

### 1. **Gradle**
- Android 项目的构建系统
- 管理依赖、编译、打包、测试等

### 2. **AndroidX**
- Android 扩展库
- 替代旧的 Support Library
- 提供向后兼容的组件

### 3. **Activity**
- Android 的界面组件
- 每个屏幕通常对应一个 Activity
- `MainActivity` 是应用的入口

### 4. **R 类**
- 自动生成的资源索引类
- 通过 `R.layout.activity_main` 访问布局
- 通过 `R.string.app_name` 访问字符串

### 5. **包名（Package Name）**
- 应用的唯一标识符
- 格式：`com.公司名.应用名`
- 本项目：`com.example.demo2`

---

## 📝 开发流程

1. **修改布局**: 编辑 `res/layout/activity_main.xml`
2. **编写逻辑**: 在 `MainActivity.java` 中实现功能
3. **添加资源**: 在 `res/` 目录添加图片、字符串等
4. **配置权限**: 在 `AndroidManifest.xml` 添加权限声明
5. **添加依赖**: 在 `app/build.gradle.kts` 添加第三方库
6. **运行测试**: 编写和运行测试用例
7. **构建应用**: 使用 Gradle 构建 APK

---

## 🚀 常用 Gradle 命令

```bash
# 清理构建
./gradlew clean

# 构建 Debug 版本
./gradlew assembleDebug

# 构建 Release 版本
./gradlew assembleRelease

# 运行单元测试
./gradlew test

# 安装到设备
./gradlew installDebug
```

---

## ⚠️ 注意事项

1. **不要修改自动生成的文件**: `.gradle/`, `build/`, `R.java` 等
2. **不要提交到 Git**: `local.properties`, `build/`, `.gradle/`
3. **同步 Gradle**: 修改 `build.gradle.kts` 后要点击 "Sync Now"
4. **包名唯一性**: 发布到 Google Play 时包名必须全球唯一

---

## 📚 学习建议

1. 先熟悉 `MainActivity.java` 和 `activity_main.xml`
2. 学习 Activity 生命周期
3. 掌握常用布局（LinearLayout, ConstraintLayout）
4. 了解 Intent 和 Activity 跳转
5. 学习资源管理和多语言支持
6. 逐步学习 Fragment、RecyclerView 等高级组件

---

**祝你 Android 开发学习顺利！** 🎉
