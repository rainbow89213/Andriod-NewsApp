# 3. Android 服务端数据下发集成说明（新手详细版）

> 本文档详细说明如何让 Android 应用的列表内容由服务端数据下发。
> 
> **适合人群**：Android 开发新手
> 
> **学习目标**：掌握 Android 网络请求的标准方案

---

## 📋 目录

1. [方案概述](#方案概述)
2. [文件修改清单](#文件修改清单)
3. [详细实现步骤](#详细实现步骤)
4. [测试流程](#测试流程)
5. [常见问题](#常见问题)
6. [扩展功能](#扩展功能)

---

## 方案概述

### 为什么需要服务端数据下发？

在之前的版本中，新闻数据是写死在 Android 代码里的（硬编码）。这样有很多问题：
- ❌ 数据更新需要重新发布应用
- ❌ 无法实时更新内容
- ❌ 所有用户看到的内容完全一样

使用服务端数据下发后：
- ✅ 数据由服务器控制，随时可以更新
- ✅ 用户打开应用就能看到最新内容
- ✅ 可以根据用户推送个性化内容
- ✅ 这是所有商业 App 的标准做法

### 技术方案

**Android 端（客户端）**：
- **Retrofit** - 网络请求库（最流行的 Android 网络库）
- **OkHttp** - HTTP 客户端（Retrofit 的底层实现）
- **Gson** - JSON 解析库（Google 官方出品）
- **Glide** - 图片加载库（支持网络图片）

**服务端**：
- **Spring Boot** - Java Web 框架（最流行的 Java 后端框架）
- **RESTful API** - 提供标准的 HTTP 接口

---

## 📁 文件修改清单

### 一、修改的文件（共 5 个）

这些是在原有项目基础上修改的文件：

#### 1. `gradle/libs.versions.toml`
- **位置**：`项目根目录/gradle/libs.versions.toml`
- **作用**：管理项目依赖的版本号
- **修改内容**：添加了 Retrofit、OkHttp、Gson、Glide 的版本配置
- **修改标记**：第三次修改 ⭐

#### 2. `app/build.gradle.kts`
- **位置**：`app/build.gradle.kts`
- **作用**：应用模块的构建配置文件
- **修改内容**：在 dependencies 中添加了网络请求相关的依赖
- **修改标记**：第三次修改 ⭐

#### 3. `app/src/main/java/com/example/demo2/NewsItem.java`
- **位置**：`app/src/main/java/com/example/demo2/NewsItem.java`
- **作用**：新闻数据模型类
- **修改内容**：
  - 将 `imageResId`（本地图片资源ID）改为 `imageUrl`（网络图片地址）
  - 添加了 `@SerializedName` 注解（用于 JSON 解析）
  - 添加了无参构造函数（Gson 反序列化需要）
- **修改标记**：第三次修改 ⭐

#### 4. `app/src/main/java/com/example/demo2/NewsAdapter.java`
- **位置**：`app/src/main/java/com/example/demo2/NewsAdapter.java`
- **作用**：RecyclerView 的适配器
- **修改内容**：
  - 导入 Glide 相关类
  - 将 `setImageResource()` 改为使用 Glide 加载网络图片
  - 添加了 `clearData()` 方法
- **修改标记**：第三次修改 ⭐

#### 5. `app/src/main/java/com/example/demo2/MainActivity.java`
- **位置**：`app/src/main/java/com/example/demo2/MainActivity.java`
- **作用**：应用主界面
- **修改内容**：
  - 导入网络请求相关类
  - 添加 `apiService` 成员变量
  - 将 `loadMockData()` 改为 `loadNewsFromServer()`
  - 实现网络请求逻辑
- **修改标记**：第三次修改 ⭐

#### 6. `app/src/main/AndroidManifest.xml`
- **位置**：`app/src/main/AndroidManifest.xml`
- **作用**：应用清单文件
- **修改内容**：
  - 添加网络权限 `INTERNET`
  - 添加网络状态权限 `ACCESS_NETWORK_STATE`
  - 添加 `usesCleartextTraffic="true"`（允许 HTTP 请求）
- **修改标记**：第三次修改 ⭐

### 二、新增的文件（共 7 个）

这些是全新创建的文件：

#### Android 端新增文件（2 个）

##### 1. `app/src/main/java/com/example/demo2/api/NewsApiService.java`
- **位置**：`app/src/main/java/com/example/demo2/api/NewsApiService.java`
- **作用**：定义 API 接口
- **说明**：
  - 这是一个 Java 接口（interface）
  - 使用 Retrofit 注解定义网络请求
  - `@GET("api/news")` 表示发起 GET 请求到 `/api/news` 路径
  - Retrofit 会自动实现这个接口，生成网络请求代码
- **为什么需要**：将网络请求的定义和实现分离，代码更清晰

##### 2. `app/src/main/java/com/example/demo2/api/RetrofitClient.java`
- **位置**：`app/src/main/java/com/example/demo2/api/RetrofitClient.java`
- **作用**：Retrofit 客户端单例类
- **说明**：
  - 使用单例模式，确保全局只有一个 Retrofit 实例
  - 配置 BASE_URL（服务器地址）
  - 配置 OkHttp 客户端（超时时间、日志拦截器）
  - 配置 Gson 转换器（自动将 JSON 转为 Java 对象）
- **为什么需要**：集中管理网络配置，避免重复创建对象

#### Spring Boot 后端新增文件（5 个）

##### 3. `backend/pom.xml`
- **位置**：`backend/pom.xml`
- **作用**：Maven 项目配置文件
- **说明**：
  - 定义项目信息（groupId、artifactId、version）
  - 声明 Spring Boot 依赖
  - 配置构建插件
- **为什么需要**：Maven 通过这个文件管理项目依赖和构建过程

##### 4. `backend/src/main/resources/application.properties`
- **位置**：`backend/src/main/resources/application.properties`
- **作用**：Spring Boot 配置文件
- **说明**：
  - 配置服务器端口（8080）
  - 配置应用名称
  - 配置日志级别
- **为什么需要**：Spring Boot 启动时会读取这个文件进行配置

##### 5. `backend/src/main/java/com/example/newsbackend/NewsBackendApplication.java`
- **位置**：`backend/src/main/java/com/example/newsbackend/NewsBackendApplication.java`
- **作用**：Spring Boot 应用启动类
- **说明**：
  - `@SpringBootApplication` 注解标识这是 Spring Boot 应用
  - `main` 方法是程序入口
  - 调用 `SpringApplication.run()` 启动应用
- **为什么需要**：这是 Spring Boot 应用的入口，必须有

##### 6. `backend/src/main/java/com/example/newsbackend/model/NewsItem.java`
- **位置**：`backend/src/main/java/com/example/newsbackend/model/NewsItem.java`
- **作用**：后端的新闻数据模型
- **说明**：
  - 使用 Lombok 注解简化代码（`@Data`、`@NoArgsConstructor`、`@AllArgsConstructor`）
  - 字段名和 Android 端的 NewsItem 保持一致
  - 会自动转换为 JSON 返回给客户端
- **为什么需要**：定义数据结构，Spring Boot 会自动将其转为 JSON

##### 7. `backend/src/main/java/com/example/newsbackend/controller/NewsController.java`
- **位置**：`backend/src/main/java/com/example/newsbackend/controller/NewsController.java`
- **作用**：API 控制器，处理 HTTP 请求
- **说明**：
  - `@RestController` 标识这是 REST API 控制器
  - `@RequestMapping("/api")` 定义基础路径
  - `@GetMapping("/news")` 处理 GET 请求
  - `@CrossOrigin` 允许跨域访问（Android 应用可以访问）
  - `getNewsList()` 方法返回新闻列表
- **为什么需要**：这是 API 的核心，处理客户端请求并返回数据

---

## 详细实现步骤

### 步骤一：修改 Android 项目依赖配置

#### 1.1 修改 `gradle/libs.versions.toml`

**文件位置**：`项目根目录/gradle/libs.versions.toml`

**在 `[versions]` 部分添加**（第 23-26 行后面）：

```toml
# ========== 第三次修改：添加网络请求库版本 ⭐ ==========
retrofit = "2.9.0"          # Retrofit 网络请求库版本
okhttp = "4.12.0"           # OkHttp HTTP客户端版本
gson = "2.10.1"             # Gson JSON解析库版本
glide = "4.16.0"            # Glide 图片加载库版本
```

**在 `[libraries]` 部分添加**（第 60 行后面）：

```toml
# ========== 第三次修改：添加网络请求库依赖 ⭐ ==========
# Retrofit：类型安全的 HTTP 客户端
# 用于简化网络请求，支持 RESTful API
retrofit = { group = "com.squareup.retrofit2", name = "retrofit", version.ref = "retrofit" }
retrofit-converter-gson = { group = "com.squareup.retrofit2", name = "converter-gson", version.ref = "retrofit" }

# OkHttp：高效的 HTTP 客户端
# Retrofit 的底层实现，支持连接池、缓存等
okhttp = { group = "com.squareup.okhttp3", name = "okhttp", version.ref = "okhttp" }
okhttp-logging = { group = "com.squareup.okhttp3", name = "logging-interceptor", version.ref = "okhttp" }

# Gson：Google 的 JSON 解析库
# 用于 JSON 和 Java 对象之间的转换
gson = { group = "com.google.code.gson", name = "gson", version.ref = "gson" }

# Glide：强大的图片加载库
# 支持网络图片加载、缓存、占位图等功能
glide = { group = "com.github.bumptech.glide", name = "glide", version.ref = "glide" }
```

**为什么这样修改**：
- Gradle 使用版本目录（Version Catalog）统一管理依赖版本
- 先在 `[versions]` 定义版本号，再在 `[libraries]` 中引用
- 这样可以避免版本号重复，便于统一升级

#### 1.2 修改 `app/build.gradle.kts`

**文件位置**：`app/build.gradle.kts`

**在 `dependencies` 部分的 `implementation(libs.cardview)` 后面添加**（第 110 行后面）：

```kotlin
    // ========== 第三次修改：添加网络请求依赖 ⭐ ==========
    // Retrofit：网络请求库
    implementation(libs.retrofit)
    implementation(libs.retrofit.converter.gson)
    
    // OkHttp：HTTP客户端
    implementation(libs.okhttp)
    implementation(libs.okhttp.logging)
    
    // Gson：JSON解析库
    implementation(libs.gson)
    
    // Glide：图片加载库
    implementation(libs.glide)
```

**修改后需要做什么**：
1. 点击 Android Studio 右上角的 "Sync Now" 按钮
2. 等待 Gradle 同步完成（下载依赖库）
3. 如果同步失败，检查网络连接或使用镜像源

---

### 步骤二：修改数据模型支持网络数据

#### 2.1 修改 `NewsItem.java`

**文件位置**：`app/src/main/java/com/example/demo2/NewsItem.java`

**修改 1：在文件开头添加导入**（第 1 行后面）：

```java
// ========== 第三次修改：导入 Gson 注解 ⭐ ==========
import com.google.gson.annotations.SerializedName;
```

**修改 2：修改类注释和字段**（第 16-30 行）：

原来的代码：
```java
public class NewsItem {
    // 新闻标题
    private String title;
    
    // 新闻内容摘要
    private String summary;
    
    // 新闻图片资源 ID（引用 drawable 中的图片）
    private int imageResId;
```

改为：
```java
public class NewsItem {
    // ========== 第三次修改：添加 Gson 注解，支持 JSON 解析 ⭐ ==========
    // 新闻标题
    @SerializedName("title")
    private String title;
    
    // 新闻内容摘要
    @SerializedName("summary")
    private String summary;
    
    // 新闻图片URL（网络图片地址）
    // 从本地资源 ID 改为网络 URL
    @SerializedName("imageUrl")
    private String imageUrl;
    
    // 发布时间
    @SerializedName("publishTime")
    private String publishTime;
    
    // 阅读数
    @SerializedName("readCount")
    private String readCount;
```

**修改 3：修改构造函数**（第 41-47 行）：

原来的代码：
```java
public NewsItem(String title, String summary, int imageResId, String publishTime, String readCount) {
    this.title = title;
    this.summary = summary;
    this.imageResId = imageResId;
    this.publishTime = publishTime;
    this.readCount = readCount;
}
```

改为：
```java
// ========== 第三次修改：修改构造函数参数 ⭐ ==========
public NewsItem(String title, String summary, String imageUrl, String publishTime, String readCount) {
    this.title = title;
    this.summary = summary;
    this.imageUrl = imageUrl;  // 改为 URL
    this.publishTime = publishTime;
    this.readCount = readCount;
}

// ========== 第三次修改：添加无参构造函数（Gson 需要）⭐ ==========
public NewsItem() {
}
```

**修改 4：修改 getter 和 setter**（第 59-62 行和第 81-83 行）：

原来的代码：
```java
public int getImageResId() {
    return imageResId;
}

public void setImageResId(int imageResId) {
    this.imageResId = imageResId;
}
```

改为：
```java
// ========== 第三次修改：修改 getter/setter ⭐ ==========
public String getImageUrl() {
    return imageUrl;
}

public void setImageUrl(String imageUrl) {
    this.imageUrl = imageUrl;
}
```

**为什么这样修改**：
- `@SerializedName` 注解告诉 Gson 如何将 JSON 字段映射到 Java 字段
- 无参构造函数是 Gson 反序列化必需的
- 将 `int imageResId` 改为 `String imageUrl` 以支持网络图片

---

### 步骤三：修改 Adapter 使用 Glide 加载网络图片

#### 3.1 修改 `NewsAdapter.java`

**文件位置**：`app/src/main/java/com/example/demo2/NewsAdapter.java`

**修改 1：在文件开头添加导入**（第 8 行后面）：

```java
// ========== 第三次修改：导入 Glide 相关类 ⭐ ==========
import com.bumptech.glide.Glide;
import com.bumptech.glide.load.resource.bitmap.RoundedCorners;
import com.bumptech.glide.request.RequestOptions;
```

**修改 2：修改 `bind()` 方法中的图片加载代码**（第 85-87 行）：

原来的代码：
```java
public void bind(NewsItem newsItem) {
    // 设置图片
    newsImage.setImageResource(newsItem.getImageResId());
```

改为：
```java
public void bind(NewsItem newsItem) {
    // ========== 第三次修改：使用 Glide 加载网络图片 ⭐ ==========
    // 使用 Glide 加载网络图片
    Glide.with(itemView.getContext())
            .load(newsItem.getImageUrl())  // 加载网络图片 URL
            .apply(new RequestOptions()
                    .placeholder(android.R.drawable.ic_menu_gallery)  // 加载中显示的占位图
                    .error(android.R.drawable.ic_menu_report_image)   // 加载失败显示的图片
                    .transform(new RoundedCorners(16)))  // 圆角处理
            .into(newsImage);
```

**修改 3：在类的最后添加新方法**（第 175 行后面）：

```java
    // ========== 第三次修改：添加清空数据方法 ⭐ ==========
    /**
     * 清空数据列表
     */
    public void clearData() {
        this.newsList.clear();
        notifyDataSetChanged();
    }
```

**为什么这样修改**：
- Glide 是专门用于加载网络图片的库，自动处理缓存、内存管理
- `placeholder()` 设置加载中的占位图，提升用户体验
- `error()` 设置加载失败时的图片，避免显示空白
- `transform()` 添加圆角效果，让图片更美观

---

### 步骤四：创建 API 接口和网络服务类

这是全新创建的文件，用于定义和管理网络请求。

#### 4.1 创建 `NewsApiService.java`

**文件位置**：`app/src/main/java/com/example/demo2/api/NewsApiService.java`

**完整代码**：

```java
package com.example.demo2.api;

import com.example.demo2.NewsItem;

import java.util.List;

import retrofit2.Call;
import retrofit2.http.GET;

/**
 * NewsApiService - 新闻 API 接口定义
 * 
 * 使用 Retrofit 定义 RESTful API 接口
 * Retrofit 会根据注解自动生成网络请求代码
 * 
 * ========== 第三次修改：新增文件 ⭐ ==========
 */
public interface NewsApiService {
    
    /**
     * 获取新闻列表
     * 
     * @GET 注解表示这是一个 GET 请求
     * 参数是相对于 Base URL 的路径
     * 
     * 完整 URL = Base URL + "api/news"
     * 例如：http://10.0.2.2:8080/api/news
     * 
     * @return 返回新闻列表的 Call 对象
     */
    @GET("api/news")
    Call<List<NewsItem>> getNewsList();
}
```

**文件说明**：
- 这是一个 Java 接口（interface），不需要实现
- Retrofit 会在运行时自动生成实现代码
- `@GET` 注解定义 HTTP 方法和路径
- `Call<List<NewsItem>>` 表示返回一个新闻列表

#### 4.2 创建 `RetrofitClient.java`

**文件位置**：`app/src/main/java/com/example/demo2/api/RetrofitClient.java`

**完整代码**：

```java
package com.example.demo2.api;

import okhttp3.OkHttpClient;
import okhttp3.logging.HttpLoggingInterceptor;
import retrofit2.Retrofit;
import retrofit2.converter.gson.GsonConverterFactory;

import java.util.concurrent.TimeUnit;

/**
 * RetrofitClient - Retrofit 客户端单例类
 * 
 * 作用：
 * 1. 创建和配置 Retrofit 实例
 * 2. 使用单例模式确保全局只有一个实例
 * 3. 配置 OkHttp 客户端（超时、日志等）
 * 
 * ========== 第三次修改：新增文件 ⭐ ==========
 */
public class RetrofitClient {
    
    // Retrofit 实例（单例）
    private static Retrofit retrofit = null;
    
    // Base URL - 服务器地址
    // 注意：
    // 1. 真机测试时使用服务器的实际 IP 地址，例如：http://192.168.1.100:8080/
    // 2. 模拟器测试时使用 10.0.2.2 代表宿主机的 localhost
    // 3. 结尾必须有斜杠 /
    private static final String BASE_URL = "http://10.0.2.2:8080/";
    
    /**
     * 获取 Retrofit 实例
     * 
     * @return Retrofit 实例
     */
    public static Retrofit getClient() {
        if (retrofit == null) {
            // 创建日志拦截器（用于调试，可以看到网络请求的详细信息）
            HttpLoggingInterceptor loggingInterceptor = new HttpLoggingInterceptor();
            loggingInterceptor.setLevel(HttpLoggingInterceptor.Level.BODY);
            
            // 配置 OkHttp 客户端
            OkHttpClient okHttpClient = new OkHttpClient.Builder()
                    .addInterceptor(loggingInterceptor)  // 添加日志拦截器
                    .connectTimeout(30, TimeUnit.SECONDS)  // 连接超时 30 秒
                    .readTimeout(30, TimeUnit.SECONDS)     // 读取超时 30 秒
                    .writeTimeout(30, TimeUnit.SECONDS)    // 写入超时 30 秒
                    .build();
            
            // 创建 Retrofit 实例
            retrofit = new Retrofit.Builder()
                    .baseUrl(BASE_URL)  // 设置 Base URL
                    .client(okHttpClient)  // 设置 OkHttp 客户端
                    .addConverterFactory(GsonConverterFactory.create())  // 添加 Gson 转换器
                    .build();
        }
        return retrofit;
    }
    
    /**
     * 获取 API 服务实例
     * 
     * @return NewsApiService 实例
     */
    public static NewsApiService getNewsApiService() {
        return getClient().create(NewsApiService.class);
    }
}
```

**文件说明**：
- 使用单例模式，避免重复创建 Retrofit 实例
- `BASE_URL` 是服务器地址，模拟器使用 `10.0.2.2`
- 配置了日志拦截器，方便调试查看网络请求
- 配置了超时时间，避免请求一直等待

**重要提示**：
- 如果使用真机测试，需要修改 `BASE_URL` 为你的电脑 IP 地址
- 查看电脑 IP 的方法：
  - Windows: 打开命令提示符，输入 `ipconfig`，查看 IPv4 地址
  - Mac: 系统偏好设置 → 网络 → 查看 IP 地址

---

### 步骤五：修改 MainActivity 实现网络请求

#### 5.1 修改 `MainActivity.java`

**文件位置**：`app/src/main/java/com/example/demo2/MainActivity.java`

**修改 1：在文件开头添加导入**（第 6 行后面）：

```java
// ========== 第三次修改：导入日志和 Toast 类 ⭐ ==========
import android.util.Log;  // 日志工具类
import android.widget.Toast;  // Toast 提示
```

**修改 2：在 Java 工具类导入后添加**（第 21 行后面）：

```java
// ========== 第三次修改：导入网络请求相关类 ⭐ ==========
import com.example.demo2.api.RetrofitClient;
import com.example.demo2.api.NewsApiService;
import retrofit2.Call;
import retrofit2.Callback;
import retrofit2.Response;
```

**修改 3：在类的成员变量部分添加**（第 41 行后面）：

```java
    // ========== 第三次修改：添加日志标签和 API 服务 ⭐ ==========
    // 日志标签
    private static final String TAG = "MainActivity";
    
    // API 服务
    private NewsApiService apiService;
```

**修改 4：修改 `onCreate()` 方法**（第 85-88 行）：

原来的代码：
```java
        // 初始化 RecyclerView
        initRecyclerView();
        
        // 加载模拟数据
        loadMockData();
```

改为：
```java
        // 初始化 RecyclerView
        initRecyclerView();
        
        // ========== 第三次修改：初始化 API 服务并从服务器加载数据 ⭐ ==========
        // 初始化 API 服务
        apiService = RetrofitClient.getNewsApiService();
        
        // 从服务器加载数据
        loadNewsFromServer();
```

**修改 5：删除 `loadMockData()` 方法，替换为 `loadNewsFromServer()` 方法**（第 128-202 行）：

删除整个 `loadMockData()` 方法，替换为：

```java
    // ========== 第三次修改：新增从服务器加载数据的方法 ⭐ ==========
    /**
     * loadNewsFromServer - 从服务器加载新闻数据
     * 
     * 使用 Retrofit 进行异步网络请求
     * 
     * 工作流程：
     * 1. 调用 apiService.getNewsList() 创建请求
     * 2. 调用 enqueue() 异步执行请求
     * 3. 请求成功时调用 onResponse()
     * 4. 请求失败时调用 onFailure()
     */
    private void loadNewsFromServer() {
        Log.d(TAG, "开始加载新闻数据...");
        
        // 发起网络请求
        apiService.getNewsList().enqueue(new Callback<List<NewsItem>>() {
            @Override
            public void onResponse(Call<List<NewsItem>> call, Response<List<NewsItem>> response) {
                // 请求成功回调
                // 注意：这里的"成功"指的是收到了服务器响应，不代表业务成功
                
                if (response.isSuccessful() && response.body() != null) {
                    // HTTP 状态码 200-299，且有数据
                    List<NewsItem> newsItems = response.body();
                    Log.d(TAG, "加载成功，获取到 " + newsItems.size() + " 条新闻");
                    
                    // 更新数据到 RecyclerView
                    newsList.clear();  // 清空旧数据
                    newsList.addAll(newsItems);  // 添加新数据
                    newsAdapter.notifyDataSetChanged();  // 通知刷新
                    
                    // 显示成功提示
                    Toast.makeText(MainActivity.this, 
                        "加载成功！获取 " + newsItems.size() + " 条新闻", 
                        Toast.LENGTH_SHORT).show();
                } else {
                    // HTTP 状态码不是 200-299，或者没有数据
                    Log.e(TAG, "请求失败：" + response.code());
                    Toast.makeText(MainActivity.this, 
                        "加载失败：" + response.code(), 
                        Toast.LENGTH_SHORT).show();
                }
            }

            @Override
            public void onFailure(Call<List<NewsItem>> call, Throwable t) {
                // 请求失败回调
                // 失败原因可能是：网络错误、超时、服务器无响应等
                
                Log.e(TAG, "网络请求失败", t);
                Toast.makeText(MainActivity.this, 
                    "网络请求失败：" + t.getMessage(), 
                    Toast.LENGTH_LONG).show();
            }
        });
    }
```

**为什么这样修改**：
- `enqueue()` 是异步方法，不会阻塞主线程
- `onResponse()` 在主线程执行，可以直接更新 UI
- `Log.d()` 记录日志，方便调试
- `Toast` 显示提示信息，让用户知道加载状态

---

### 步骤六：添加网络权限

#### 6.1 修改 `AndroidManifest.xml`

**文件位置**：`app/src/main/AndroidManifest.xml`

**修改 1：在 `<manifest>` 标签内，`<application>` 标签前添加**（第 18-22 行）：

原来的代码：
```xml
    <!-- 
        如果应用需要权限，在这里声明，例如：
        <uses-permission android:name="android.permission.INTERNET" />
        <uses-permission android:name="android.permission.CAMERA" />
    -->

    <!-- application 标签：定义应用的全局配置 -->
```

改为：
```xml
    <!-- ========== 第三次修改：添加网络权限 ⭐ ========== -->
    <!-- 
        权限声明：应用需要的权限
    -->
    <!-- 网络权限：允许应用访问网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 网络状态权限：允许应用检查网络连接状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- application 标签：定义应用的全局配置 -->
```

**修改 2：在 `<application>` 标签中添加属性**（第 33 行）：

原来的代码：
```xml
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo2">
```

改为：
```xml
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo2"
        android:usesCleartextTraffic="true">
        <!-- ========== 第三次修改：允许 HTTP 明文传输 ⭐ ========== -->
```

**为什么这样修改**：
- `INTERNET` 权限是访问网络的必需权限
- `ACCESS_NETWORK_STATE` 权限用于检查网络连接状态
- `usesCleartextTraffic="true"` 允许使用 HTTP（Android 9+ 默认只允许 HTTPS）

---

## 🚀 Spring Boot 后端创建

现在创建后端服务，为 Android 应用提供数据接口。

### 步骤七：创建后端项目文件

#### 7.1 创建 `pom.xml`

**文件位置**：`backend/pom.xml`

**完整代码**：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Spring Boot 父项目 -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.0</version>
        <relativePath/>
    </parent>

    <!-- 项目信息 -->
    <groupId>com.example</groupId>
    <artifactId>news-backend</artifactId>
    <version>1.0.0</version>
    <name>news-backend</name>
    <description>新闻列表后端服务</description>

    <!-- Java 版本 -->
    <properties>
        <java.version>17</java.version>
    </properties>

    <!-- 依赖 -->
    <dependencies>
        <!-- Spring Boot Web Starter -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Lombok：简化 Java 代码 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <!-- 构建配置 -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
```

**文件说明**：
- Maven 项目配置文件
- 定义了 Spring Boot 版本（3.2.0）和 Java 版本（17）
- 添加了 Web 和 Lombok 依赖

#### 7.2 创建 `application.properties`

**文件位置**：`backend/src/main/resources/application.properties`

**完整代码**：

```properties
# 服务器端口配置
server.port=8080

# 应用名称
spring.application.name=news-backend

# 日志级别
logging.level.root=INFO
logging.level.com.example=DEBUG
```

**文件说明**：
- Spring Boot 配置文件
- 配置服务器端口为 8080
- 配置日志级别

#### 7.3 创建 `NewsBackendApplication.java`

**文件位置**：`backend/src/main/java/com/example/newsbackend/NewsBackendApplication.java`

**完整代码**：

```java
package com.example.newsbackend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Spring Boot 应用启动类
 * 
 * ========== 第三次修改：新增文件 ⭐ ==========
 */
@SpringBootApplication
public class NewsBackendApplication {

    public static void main(String[] args) {
        SpringApplication.run(NewsBackendApplication.class, args);
        System.out.println("===========================================");
        System.out.println("新闻后端服务已启动！");
        System.out.println("访问地址: http://localhost:8080/api/news");
        System.out.println("===========================================");
    }
}
```

**文件说明**：
- Spring Boot 应用的入口类
- `@SpringBootApplication` 是核心注解
- `main` 方法启动应用

#### 7.4 创建 `NewsItem.java`（后端模型）

**文件位置**：`backend/src/main/java/com/example/newsbackend/model/NewsItem.java`

**完整代码**：

```java
package com.example.newsbackend.model;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

/**
 * 新闻数据模型
 * 
 * 使用 Lombok 注解简化代码：
 * @Data - 自动生成 getter、setter、toString、equals、hashCode
 * @NoArgsConstructor - 生成无参构造函数
 * @AllArgsConstructor - 生成全参构造函数
 * 
 * ========== 第三次修改：新增文件 ⭐ ==========
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class NewsItem {
    
    /**
     * 新闻标题
     */
    private String title;
    
    /**
     * 新闻摘要
     */
    private String summary;
    
    /**
     * 图片 URL
     */
    private String imageUrl;
    
    /**
     * 发布时间
     */
    private String publishTime;
    
    /**
     * 阅读数
     */
    private String readCount;
}
```

**文件说明**：
- 后端的数据模型，和 Android 端保持一致
- 使用 Lombok 简化代码
- Spring Boot 会自动将其转为 JSON

#### 7.5 创建 `NewsController.java`

**文件位置**：`backend/src/main/java/com/example/newsbackend/controller/NewsController.java`

**完整代码**：

```java
package com.example.newsbackend.controller;

import com.example.newsbackend.model.NewsItem;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.ArrayList;
import java.util.List;

/**
 * 新闻 API 控制器
 * 
 * @RestController - 标识这是一个 REST API 控制器
 * @RequestMapping - 定义基础路径
 * @CrossOrigin - 允许跨域请求（允许 Android 应用访问）
 * 
 * ========== 第三次修改：新增文件 ⭐ ==========
 */
@RestController
@RequestMapping("/api")
@CrossOrigin(origins = "*")
public class NewsController {
    
    /**
     * 获取新闻列表
     * 
     * @GetMapping - 处理 GET 请求
     * 访问路径：http://localhost:8080/api/news
     * 
     * @return 新闻列表
     */
    @GetMapping("/news")
    public List<NewsItem> getNewsList() {
        List<NewsItem> newsList = new ArrayList<>();
        
        // 模拟新闻数据
        // 在实际项目中，这些数据应该从数据库获取
        
        newsList.add(new NewsItem(
            "科技前沿：人工智能技术取得重大突破",
            "最新研究表明，深度学习算法在图像识别领域达到了新的里程碑，准确率提升至99.8%。",
            "https://picsum.photos/400/200?random=1",
            "2小时前",
            "1.2万阅读"
        ));
        
        newsList.add(new NewsItem(
            "经济观察：全球市场迎来复苏信号",
            "多个国际机构发布报告显示，全球经济正在逐步恢复，预计今年GDP增长率将达到3.5%。",
            "https://picsum.photos/400/200?random=2",
            "5小时前",
            "8.6万阅读"
        ));
        
        newsList.add(new NewsItem(
            "体育快讯：国足晋级世界杯预选赛下一轮",
            "在昨晚进行的关键比赛中，国家队以2:1战胜对手，成功晋级世界杯预选赛下一轮。",
            "https://picsum.photos/400/200?random=3",
            "8小时前",
            "15.3万阅读"
        ));
        
        newsList.add(new NewsItem(
            "健康生活：专家建议每天运动30分钟",
            "世界卫生组织最新指南指出，每天至少进行30分钟中等强度运动，可以显著降低心血管疾病风险。",
            "https://picsum.photos/400/200?random=4",
            "12小时前",
            "5.7万阅读"
        ));
        
        newsList.add(new NewsItem(
            "娱乐八卦：知名导演新片获国际大奖",
            "在刚刚结束的国际电影节上，华语导演的最新作品斩获最佳影片奖。",
            "https://picsum.photos/400/200?random=5",
            "1天前",
            "23.4万阅读"
        ));
        
        newsList.add(new NewsItem(
            "教育资讯：在线教育平台用户突破千万",
            "随着数字化教育的普及，国内领先的在线教育平台宣布注册用户突破1000万。",
            "https://picsum.photos/400/200?random=6",
            "1天前",
            "4.2万阅读"
        ));
        
        newsList.add(new NewsItem(
            "环保行动：多地启动碳中和试点项目",
            "为应对气候变化，全国多个城市启动碳中和试点项目，力争2030年实现碳达峰目标。",
            "https://picsum.photos/400/200?random=7",
            "2天前",
            "6.8万阅读"
        ));
        
        newsList.add(new NewsItem(
            "美食推荐：网红餐厅排队3小时仍火爆",
            "位于市中心的一家网红餐厅持续火爆，即使需要排队3小时，食客们依然络绎不绝。",
            "https://picsum.photos/400/200?random=8",
            "2天前",
            "9.1万阅读"
        ));
        
        return newsList;
    }
}
```

**文件说明**：
- API 控制器，处理 HTTP 请求
- `@GetMapping("/news")` 定义 GET 请求路径
- `@CrossOrigin` 允许 Android 应用跨域访问
- 返回的数据会自动转为 JSON

---

## 🧪 测试流程

### 步骤一：启动后端服务

#### 1.1 使用命令行启动

打开命令提示符或终端，执行：

```bash
# 进入后端目录
cd d:\Gradetwo\andriod\demo2\backend

# 使用 Maven 运行
mvn spring-boot:run
```

**看到以下信息表示启动成功**：

```
===========================================
新闻后端服务已启动！
访问地址: http://localhost:8080/api/news
===========================================
```

#### 1.2 验证 API 接口

打开浏览器，访问：http://localhost:8080/api/news

**应该看到 JSON 格式的新闻列表**：

```json
[
  {
    "title": "科技前沿：人工智能技术取得重大突破",
    "summary": "最新研究表明...",
    "imageUrl": "https://picsum.photos/400/200?random=1",
    "publishTime": "2小时前",
    "readCount": "1.2万阅读"
  },
  ...
]
```

如果看到这个 JSON 数据，说明后端服务正常运行！

---

### 步骤二：运行 Android 应用

#### 2.1 使用模拟器测试

1. 确保后端服务已启动
2. 在 Android Studio 中点击运行按钮
3. 选择模拟器
4. 等待应用启动

**预期结果**：
- 应用启动后自动加载新闻列表
- 显示 Toast 提示："加载成功！获取 8 条新闻"
- 新闻列表显示网络图片

#### 2.2 使用真机测试

**前提条件**：
- 手机和电脑在同一个 WiFi 网络
- 查看电脑 IP 地址（例如：192.168.1.100）

**步骤**：

1. 修改 `RetrofitClient.java` 中的 BASE_URL：

```java
// 将 10.0.2.2 改为你的电脑 IP
private static final String BASE_URL = "http://192.168.1.100:8080/";
```

2. 重新运行应用

**预期结果**：
- 和模拟器测试相同

---

### 步骤三：查看日志

#### 3.1 Android 端日志

在 Android Studio 底部打开 Logcat，搜索 "MainActivity"：

**成功时的日志**：
```
D/MainActivity: 开始加载新闻数据...
D/MainActivity: 加载成功，获取到 8 条新闻
```

**失败时的日志**：
```
E/MainActivity: 网络请求失败
E/MainActivity: java.net.ConnectException: Failed to connect to /10.0.2.2:8080
```

#### 3.2 后端日志

在后端控制台可以看到请求日志：

```
2024-11-19 01:30:15.123  INFO 12345 --- [nio-8080-exec-1] o.s.web.servlet.DispatcherServlet        : Completed initialization
2024-11-19 01:30:15.456  INFO 12345 --- [nio-8080-exec-2] c.e.n.controller.NewsController          : GET /api/news
```

---

## ❓ 常见问题

### 问题 1：网络请求失败 - Failed to connect

**错误信息**：
```
java.net.ConnectException: Failed to connect to /10.0.2.2:8080
```

**可能原因**：
1. 后端服务没有启动
2. 端口号不对
3. 防火墙阻止了连接

**解决方案**：
1. 检查后端服务是否启动（浏览器访问 http://localhost:8080/api/news）
2. 确认端口号是 8080
3. 关闭防火墙或添加例外规则
4. 真机测试时确保手机和电脑在同一网络

---

### 问题 2：图片加载失败

**现象**：
- 新闻列表显示正常
- 图片位置显示占位图或错误图

**可能原因**：
1. 网络连接问题
2. 图片 URL 无效
3. 没有添加 INTERNET 权限

**解决方案**：
1. 检查网络连接
2. 在浏览器中测试图片 URL 是否有效
3. 确认 AndroidManifest.xml 中添加了 INTERNET 权限

---

### 问题 3：JSON 解析错误

**错误信息**：
```
com.google.gson.JsonSyntaxException: Expected BEGIN_OBJECT but was BEGIN_ARRAY
```

**可能原因**：
- Android 端和后端的数据模型不一致
- `@SerializedName` 注解错误

**解决方案**：
1. 确保 Android 端和后端的字段名完全一致
2. 检查 `@SerializedName` 注解是否正确
3. 在 Logcat 中查看实际返回的 JSON 格式

---

### 问题 4：后端启动失败

**错误信息**：
```
Error: Could not find or load main class com.example.newsbackend.NewsBackendApplication
```

**可能原因**：
1. Maven 依赖没有下载完成
2. 项目结构不正确
3. Java 版本不对

**解决方案**：
1. 运行 `mvn clean install` 重新构建
2. 检查文件路径是否正确
3. 确认 Java 版本是 17 或更高

---

### 问题 5：Gradle 同步失败

**错误信息**：
```
Could not resolve com.squareup.retrofit2:retrofit:2.9.0
```

**可能原因**：
- 网络连接问题
- Maven 仓库无法访问

**解决方案**：
1. 检查网络连接
2. 配置国内镜像源（阿里云、腾讯云）
3. 使用 VPN

---

## 🎯 总结

### 完成的工作

✅ **修改的文件（6个）**：
1. `gradle/libs.versions.toml` - 添加依赖版本
2. `app/build.gradle.kts` - 添加依赖引用
3. `NewsItem.java` - 支持网络图片和 JSON 解析
4. `NewsAdapter.java` - 使用 Glide 加载网络图片
5. `MainActivity.java` - 实现网络请求
6. `AndroidManifest.xml` - 添加网络权限

✅ **新增的文件（7个）**：
1. `NewsApiService.java` - API 接口定义
2. `RetrofitClient.java` - Retrofit 客户端
3. `pom.xml` - Maven 配置
4. `application.properties` - Spring Boot 配置
5. `NewsBackendApplication.java` - 后端启动类
6. `NewsItem.java`（后端）- 后端数据模型
7. `NewsController.java` - API 控制器

### 技术要点

- **Retrofit** - Android 最流行的网络请求库
- **Glide** - 强大的图片加载库
- **Spring Boot** - 快速构建后端服务
- **RESTful API** - 标准的 HTTP 接口设计
- **JSON** - 数据交换格式

### 下一步建议

1. **添加下拉刷新** - 使用 SwipeRefreshLayout
2. **添加分页加载** - 实现上拉加载更多
3. **添加缓存** - 使用 Room 数据库缓存数据
4. **连接真实数据库** - 后端连接 MySQL
5. **添加错误处理** - 更完善的错误提示

---

## 📚 参考资料

- [Retrofit 官方文档](https://square.github.io/retrofit/)
- [Glide 官方文档](https://bumptech.github.io/glide/)
- [Spring Boot 官方文档](https://spring.io/projects/spring-boot)
- [Android 官方文档](https://developer.android.com/)

---

**恭喜你完成了服务端数据下发的集成！🎉**

现在你的 Android 应用已经可以从服务器获取数据了，这是所有商业应用的基础功能。继续加油！💪
